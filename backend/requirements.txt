# ============================================================
# CyberRemote Monitor Pro — Dependencias del MVP (EPIC 1)
# Stack mínimo para API + PostgreSQL/SQLite. Sin scrapers pesados.
# ============================================================

fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.20.0          # SQLite async — dev local sin infraestructura
asyncpg==0.29.0            # PostgreSQL async — Docker Compose
pydantic-settings==2.2.1
python-dotenv==1.0.1
httpx==0.27.0              # usado por conectores RemoteOK y TestClient

# ─── Testing ──────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7

# ─── EPICs futuros (no requeridos por el MVP) ────────────────
# celery[redis], geoalchemy2, alembic, playwright, pandas,
# openai, reportlab, openpyxl → se añaden según roadmap
